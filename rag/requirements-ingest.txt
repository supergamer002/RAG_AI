# Dipendenze per il layer di ingestion (rag/ingest/, rag/common/).
# Il resto dello stack (embedding, LanceDB, reranker, generazione) avra'
# il proprio requirements quando sviluppato.
docling>=2.0
requests>=2.31
lancedb>=0.15
pyarrow>=16
tantivy>=0.22
FlagEmbedding>=1.3  # reranking (BAAI/bge-reranker-v2-m3), non richiesto dagli altri moduli
pypdf>=4.0  # controllo layer testo per skip OCR in docling_extract.py
umap-learn>=0.5.7  # riduzione dimensionale UMAP per il Vector Explorer
